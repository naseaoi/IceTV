# 性能优化待办清单

目标：让用户明显感受到页面响应更快，同时降低长期运行容器的 CPU、内存和网络占用。

## 审计基线

浏览器测试环境：Next.js 开发模式、移动端 `390x844`、DPR 3、Fast 4G、CPU 4 倍降速。

- 已登录首页 LCP 约 1794ms，TTFB 约 503ms
- LCP 渲染延迟约 1290ms，占 LCP 的约 72%
- 已登录首页 DOM 约 2416 个节点，首轮创建约 36 张图片
- 未登录首页 DOM 约 1953 个节点，首轮创建约 33 张图片
- Douban 页面静止性能轨迹中，`VirtualizedGrid.readViewport` 累计触发约 488ms 强制回流
- 未登录首页已不再请求 `/api/playrecords`，但仍请求 `/api/server-config`
- Docker 健康检查每 30 秒请求完整 `/login` 页面
- 开发模式下 `/login` 响应约 178831 字符，单次请求约 2 秒

开发模式绝对值不作为生产指标。每批改动完成后，需要在现有开发环境验证功能；最终使用 production standalone 固定数据集复测。

## 第一批：低风险运行开销

- [x] 增加轻量健康检查端点
  - [x] Docker HEALTHCHECK 改用轻量端点
  - [x] `start.js` 启动就绪探测改用轻量端点
  - [x] 验证健康端点不读取页面配置、不渲染 React 页面
- [x] 移除 `VirtualizedGrid` 常驻 120ms 轮询
  - [x] 保留滚动、窗口尺寸和容器尺寸变化监听
  - [x] 验证搜索页和 Douban 页面虚拟列表滚动定位
- [x] 移除 `useInfiniteScroll` 常驻 160ms 轮询
  - [x] 使用 IntersectionObserver 和真实滚动事件触发
  - [x] 验证自动加载下一页及快速滚动场景
- [x] 调整封面预加载范围
  - [x] 横向预加载范围小于纵向预加载范围
  - [x] 弱网或省流模式进一步收紧
  - [x] 保留现有图片并发调度能力

### 第一批验收

- [x] 相关单元测试通过
- [x] lint 和 typecheck 通过
- [x] Douban 页面静止轨迹不再出现轮询导致的持续强制回流
- [x] 首页图片仍能在滚动前及时加载，无明显白屏
- [x] 提交第一批改动，不推送

## 第二批：首页首屏和重复请求

- [x] 降低首页首屏卡片挂载数量
  - [x] 每个横向推荐区首批只挂载视口附近卡片
  - [x] 横向滚动时渐进挂载剩余卡片
  - [x] 保留进入分类页所需的客户端数据缓存
- [x] 降低首页复杂卡片的样式和布局成本
  - [x] 评估 `content-visibility` 与固有尺寸占位
  - [x] 验证横向滚动、悬停和移动端长按交互
- [x] 首页继续观看只读取展示需要的最近记录
  - [x] 增加最近播放记录读取能力
  - [x] 避免首页下载、解析并排序全部播放记录
  - [x] 保留其他页面读取完整记录的能力
- [ ] 去除游客首屏重复 `/api/server-config` 请求
  - [ ] 根布局注入的数据直接作为游客最终运行时配置
  - [ ] 登录用户仍能得到受保护的代理配置
  - [ ] 管理后台保存配置后仍能显式刷新客户端配置
- [ ] 服务端会话校验做请求级复用
  - [ ] 根布局与首页继续观看骨架复用同一校验结果
  - [ ] 禁止跨用户全局缓存会话结果

### 第二批验收

- [ ] 首页未登录和已登录场景浏览器验证通过
- [ ] 首页 DOM、图片请求数和主文档体积相较基线下降
- [ ] 继续观看、分类跳转和公告功能正常
- [ ] 相关测试、lint 和 typecheck 通过
- [ ] 提交第二批改动，不推送

## 第三批：后台任务和容器峰值

- [ ] cron 增加执行锁
  - [ ] 单进程内禁止重叠执行
  - [ ] 多容器部署时具备可扩展的租约或分布式锁方案
- [ ] 启动时不再立即执行全量维护
  - [ ] 延迟首次维护任务
  - [ ] 避免首次用户请求与 cron 争抢资源
- [ ] 拆分不同维护周期
  - [ ] 配置订阅刷新
  - [ ] 直播源刷新
  - [ ] 播放记录和收藏元数据刷新
- [ ] 播放记录和收藏改为增量刷新
  - [ ] 保存最近元数据刷新时间
  - [ ] 仅刷新可能变化且超过 TTL 的条目
  - [ ] 单次任务具备条目上限和时间预算
- [ ] 已知 `source + id` 时优先直接获取详情
  - [ ] 直接详情失败后才回退标题搜索
  - [ ] 保留同一轮任务中的请求去重

### 第三批验收

- [ ] cron 重复触发不会并行执行
- [ ] 小规模数据下不会每小时全量回源
- [ ] 记录集规模增大时任务仍受并发和时间预算限制
- [ ] 相关测试、lint 和 typecheck 通过
- [ ] 提交第三批改动，不推送

## 第四批：直播 EPG 和流量路由

- [ ] 直播源刷新限制并发为 1 到 2
- [ ] EPG 改为异步或流式解压
- [ ] EPG 解析避免同时持有多份完整大文本
- [ ] 仅保留当前附近和未来有限时间窗口的节目单
- [ ] 直播缓存使用 stale-while-revalidate
  - [ ] 过期时先返回旧缓存
  - [ ] 刷新成功后原子替换
  - [ ] 刷新失败不删除可用缓存
- [ ] 对支持 CORS 的直播源自动尝试浏览器直连
  - [ ] 探测失败自动回退服务端代理
  - [ ] 不要求用户安装 CORS 插件才能正常播放
  - [ ] 保留手动强制服务端代理能力

### 第四批验收

- [ ] 大 EPG 文件不会长时间阻塞 Node.js 事件循环
- [ ] 多直播源刷新时峰值内存受控
- [ ] 直播频道和节目单切换正常
- [ ] 直连失败时能自动恢复服务端播放
- [ ] 提交第四批改动，不推送

## 第五批：缓存、长期数据和高频路由

- [ ] `createSwrCache` 增加字节或权重预算
  - [ ] 支持 `maxWeightBytes`
  - [ ] 支持调用方提供 `estimateWeight`
  - [ ] 写入前清理硬过期条目
  - [ ] 超预算时按 LRU 淘汰
- [ ] 为 detail、episode URL、m3u8 和重写缓存设置合理预算
- [ ] 增加缓存条目数、估算字节、命中和未命中统计
- [ ] 播放统计会话增加长期保留策略
  - [ ] 定期清理超期明细
  - [ ] 需要长期统计时保留按日汇总数据
- [ ] 审核高频媒体路由的重复鉴权
  - [ ] 已有独立代理签名的路由避免重复会话验签
  - [ ] 不降低任何未签名 API 的访问边界
- [ ] 审核侧栏全量路由预取
  - [ ] 生产环境确认实际 RSC 预取请求
  - [ ] 改为 hover、focus 或明确意图预取

### 第五批验收

- [ ] 缓存压力测试不会超过配置的字节预算
- [ ] 播放、搜索、详情和代理缓存行为保持正确
- [ ] 高频媒体请求鉴权边界测试通过
- [ ] 相关测试、lint 和 typecheck 通过
- [ ] 提交第五批改动，不推送

## 最终验收

- [ ] 全量 Jest 测试通过
- [ ] 全量 lint 通过
- [ ] typecheck 通过
- [ ] `git diff --check` 通过
- [ ] production standalone 固定数据集复测
  - [ ] 首页 TTFB、LCP、INP
  - [ ] 首屏请求数、图片字节和主文档体积
  - [ ] 容器空闲 CPU 和 RSS
  - [ ] cron 执行时长、外部请求数和峰值 RSS
  - [ ] 直播每小时容器出口流量
- [ ] 更新本清单完成状态
- [ ] 最终提交，不推送
